# ZenitarParser — Telegram Chat Parser

**ZenitarParser** — это парсер публичных чатов Telegram, который ищет сообщения по ключевым словам и сохраняет найденные `username` пользователей в CSV-файл.
Работает через Telegram-бота. Никаких дополнительных сессий создавать не нужно — достаточно указать свои данные в конфиге.

---

## 🚀 Возможности

* 🔎 Поиск по ключевым словам в публичных чатах
* 📄 Экспорт `username` в CSV
* ⏹ Остановка парсинга вручную
* 🤖 Управление через бота

---

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/SyberianIT/ZenitarParser_bot-Python-Telegram.git
cd ZenitarParser_bot-Python-Telegram
```

---

### 2. Создание виртуального окружения (рекомендуется)

#### Linux / macOS

```bash
python3.12 -m venv venv
source venv/bin/activate
```

#### Windows (CMD)

```cmd
python -m venv venv
venv\Scripts\activate
```

#### Windows (PowerShell)

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

---

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## ⚙ Настройка

Настройки подставьте в файл `config.py` в корне проекта и укажите **свои данные**:

```python
API_ID = 12345
API_HASH = "ваш_api_hash"
BOT_TOKEN = "токен_бота"
ADMIN_ID = 123456789
```

Где взять данные:

* `API_ID` и `API_HASH` — на my.telegram.org
* `BOT_TOKEN` — через бота BotFather
* `ADMIN_ID` — ваш Telegram ID

⚠ Ничего дополнительно создавать не нужно. Просто подставьте свои данные.

---

## ▶ Запуск

```bash
python app.py
```

После запуска бот будет готов к работе.

---

## 📖 Использование

В Telegram:

* `/start` — открыть меню
* **«Парсер»** — ввести ключевые слова (каждое с новой строки)
* **«Остановить»** — остановить поиск

После завершения бот отправит CSV-файл с найденными `username`.

---

## 🏷 Пример ключевых слов

```
впн
python
крипта
новости
фильмы
music
games
фриланс
```

Каждое слово — с новой строки.

---

## 📁 Структура проекта

```
ZenitarParser/
├── app.py
├── config.py
├── requirements.txt
├── simple_test.py
├── sessions/ <-- Сюда ставить .session Pyrogram
```
---

## ⚠ Возможные ошибки

### ModuleNotFoundError

Активируйте виртуальное окружение и установите зависимости:

```bash
pip install -r requirements.txt
```

### Проблемы с авторизацией

Проверьте корректность `API_ID`, `API_HASH` и `BOT_TOKEN`.

---

## 👤 Автор

SyberianIT

## 📄 Лицензия

MIT
